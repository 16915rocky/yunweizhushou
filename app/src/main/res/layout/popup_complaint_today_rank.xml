<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="20dp"
    android:background="@drawable/corner_rectangle_white_bg2"
    android:orientation="vertical"
    android:padding="10dp" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal" >

        <TextView
            android:id="@+id/complaint_today_rank_pupop_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:padding="5dp"
            android:text="1111111"
            android:textColor="@color/color_lightblue" />

        <ImageView
            android:id="@+id/popup_cancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="5dp"
            android:src="@mipmap/icon_chacha" />
    </LinearLayout>

    <ListView
        android:id="@+id/complaint_today_rank_pupop_list"
        android:layout_width="match_parent"
        android:layout_height="233dp"
        android:divider="@null"
        android:scrollbars="none" >
    </ListView>

</LinearLayout>